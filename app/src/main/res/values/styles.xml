<resources>

    <!-- Base application theme. -->
    <style name="JKMovie" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/black_primary</item>
        <item name="android:textColorHint">@color/black_hint_and_disable</item>
        <item name="android:textColorSecondary">@color/black_secondary</item>
        <item name="android:windowBackground">@color/white_primary</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementsUseOverlay">false</item>
    </style>

    <style name="JKMovie.TransSysBar">
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent
        </item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="JKMovie.FullScreen">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="JKMovieTheme.Toolbar" parent="Widget.AppCompat.Toolbar">
    </style>

    <style name="Widget.JKMovie.RatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:minHeight">24dip</item>
        <item name="android:maxHeight">24dip</item>
    </style>

    <style name="TextAppearance.Primary" parent="">
        <item name="android:textColor">@color/white_primary</item>
    </style>

    <style name="TextAppearance.Primary.Inverse">
        <item name="android:textColor">@color/black_primary</item>
    </style>


</resources>
